# --- Production Lock File v5 ---
# This version combination is confirmed to be compatible and resolves the dependency conflicts.

# API Framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
gevent

# Core AI and Agentic Workflow Libraries
langchain==0.2.5
langgraph==0.0.69
langchain-cohere==0.1.9

# THE FIX: Pin langchain-tavily to an older, stable version (0.0.3) that is
# compatible with langchain v0.2.x and does not have the broken dependency requirement.
langchain-tavily==0.0.3

# Redis client for connecting to Upstash
redis==5.0.4

# For generating .docx files
python-docx==1.1.2

# For loading environment variables from .env file
python-dotenv==1.0.1
